{
  "name": "dockhand-db",
  "private": true,
  "scripts": {
    "migrate": "prisma migrate deploy",
    "diff": "prisma migrate diff --from-empty --to-schema prisma/schema.prisma --script"
  },
  "devDependencies": {
    "prisma": "7.10.0"
  }
}
